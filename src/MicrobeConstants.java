/**
 * MicrobeConstants interface including static constants needed
 * for creating the simulation window for the microbes
 */
public interface MicrobeConstants {
	
    // the size of the PetriDish;
    // and y coordinates run from 0 to 19
    public static final int SIZE = 20;
    // the number of pixels corresponding on one x and y coordinate
    // unit i.e., you PetriDish areas has a size of 200x200 pixels
    public static final int PIXELS = 10;
}
